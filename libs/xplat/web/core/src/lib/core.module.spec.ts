describe('Core module generic test', () => {
  it('Should be true', () => {
    expect(true).toBeTruthy();
  });
});

/*
import { XplatDemoCoreModule } from './core.module';

describe('XplatDemoCoreModule', () => {
  it('should work', () => {
    expect(new XplatDemoCoreModule(null)).toBeDefined();
  });
});
*/
